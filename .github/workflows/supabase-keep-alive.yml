name: Supabase Keep-Alive

on:
  schedule:
    - cron: '0 12 */5 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s "https://ileauttjsshfrihianpt.supabase.co/rest/v1/"
          echo "Pinged Supabase successfully"
